enum Timing {
  part_time = 'part-time',
  half_time = 'half-time',
  full_time = 'full-time',
  flexible_time = 'flexible-time',
  tasks = 'tasks',
  remote = 'remote',
  contract = 'contract',
}

export default Timing;
